{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "The context of an instance record",
  "type": "object",
  "properties": {
    "@context": {
      "description": "Context of an instance record",
      "type": "object",
      "properties": {
        "dcterms": {
          "description": "Dcterms",
          "type": "string"
        },
        "title": {
          "description": "Title",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "dcterms",
        "title"
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "@context"
  ]
}
